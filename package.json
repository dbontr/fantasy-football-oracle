{
  "name": "fantasy-football-oracle",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-first fantasy football draft, trade, waiver, and roster assistant",
  "scripts": {
    "build:data": "node scripts/build-player-data.js",
    "check": "node --check app-core.js && node --check app.js && node --check scripts/build-player-data.js && node --check tools/espn-oracle.user.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
